
version: 2.1
 
orbs:
  python: circleci/python@0.2.1
 
jobs:
  klj-automated-tests:
    executor: python/default
    steps:
      - checkout
      - run:
          command: |
            pip install pipenv
            pipenv install
            pipenv run python main.py
          name: Test
      - store_test_results:
          path: ./report/
 
      - store_artifacts:
          path: ./report/
          destination: reports
 
workflows:
  main:
    jobs:
      - klj-automated-tests